/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ActividadAddComponent } from './actividad-add.component';

describe('ActividadAddComponent', () => {
  let component: ActividadAddComponent;
  let fixture: ComponentFixture<ActividadAddComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ActividadAddComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ActividadAddComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
