/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { GestionDeEmpleadosComponent } from './gestion-de-empleados.component';

describe('GestionDeEmpleadosComponent', () => {
  let component: GestionDeEmpleadosComponent;
  let fixture: ComponentFixture<GestionDeEmpleadosComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ GestionDeEmpleadosComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(GestionDeEmpleadosComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
