/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { personaacargoactividadAddEditComponent } from './personaacargoactividad-add-edit.component';

describe('PersonaacargoactividadAddEditComponent', () => {
  let component: personaacargoactividadAddEditComponent;
  let fixture: ComponentFixture<personaacargoactividadAddEditComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ personaacargoactividadAddEditComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(personaacargoactividadAddEditComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
