/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { PersonaacargoactividadComponent } from './personaacargoactividad.component';

describe('PersonaacargoactividadComponent', () => {
  let component: PersonaacargoactividadComponent;
  let fixture: ComponentFixture<PersonaacargoactividadComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ PersonaacargoactividadComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(PersonaacargoactividadComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
