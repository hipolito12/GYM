/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { RutinasPersoComponent } from './rutinas-perso.component';

describe('RutinasPersoComponent', () => {
  let component: RutinasPersoComponent;
  let fixture: ComponentFixture<RutinasPersoComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ RutinasPersoComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(RutinasPersoComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
