/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { RutinasUpdateComponent } from './rutinas-update.component';

describe('RutinasUpdateComponent', () => {
  let component: RutinasUpdateComponent;
  let fixture: ComponentFixture<RutinasUpdateComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ RutinasUpdateComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(RutinasUpdateComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
