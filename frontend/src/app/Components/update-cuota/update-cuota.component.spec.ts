/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { UpdateCuotaComponent } from './update-cuota.component';

describe('UpdateCuotaComponent', () => {
  let component: UpdateCuotaComponent;
  let fixture: ComponentFixture<UpdateCuotaComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ UpdateCuotaComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(UpdateCuotaComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
