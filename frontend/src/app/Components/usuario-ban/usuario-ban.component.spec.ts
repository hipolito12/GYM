/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { UsuarioBanComponent } from './usuario-ban.component';

describe('UsuarioBanComponent', () => {
  let component: UsuarioBanComponent;
  let fixture: ComponentFixture<UsuarioBanComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ UsuarioBanComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(UsuarioBanComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
