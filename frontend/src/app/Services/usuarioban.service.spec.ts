/* import { TestBed } from '@angular/core/testing';

import { UsuariobanService } from './usuarioban.service';

describe('UsuariobanService', () => {
  let service: UsuariobanService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(UsuariobanService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
 */
